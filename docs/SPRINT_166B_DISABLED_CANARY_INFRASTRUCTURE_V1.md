# Sprint 166B — Disabled Canary Infrastructure

**Status:** Preview-only configuration prepared and verified live, then — in a later, separately-approved phase of this same sprint — activated for exactly one controlled canary write. See "Canary write execution result" below for the full record. Both kill switches were returned to `false` immediately after. Not merged to `main`.

---

## State before this sprint

Only the four Sprint 165C isolation variables existed on Preview: `SUPABASE_ENVIRONMENT_TAG`, `SUPABASE_EXPECTED_PROJECT_REF`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. None of the candidate-automation variables existed anywhere, at any scope. `CRON_SECRET` and `SCHEDULED_CHECKS_ENABLED` existed but only in **Production** scope, untouched by this sprint.

## Non-secret variables set this sprint (all Preview scope, all Preview branches, no custom-branch restriction)

| Variable | Value | Purpose |
|---|---|---|
| `SCHEDULED_CHECKS_ENABLED` | `false` | Kill switch layer 1 — explicitly OFF |
| `SCHEDULED_WRITES_ENABLED` | `false` | Kill switch layer 2 — explicitly OFF |
| `SCHEDULED_WRITER_FINGERPRINT_ENABLED` | `true` | Enables the DB-level unique-constraint race guard (column already exists on `alertownik-preview`'s replayed schema) |
| `SCHEDULED_WRITER_MAX_CANDIDATES_PER_RUN` | `1` | Explicit hard cap, matching the code's own safe default |
| `SCHEDULED_WRITER_ALLOWED_SOURCE_IDS` | `["michalowice-komunikaty"]` | Single-source allowlist for a future write run |
| `SCHEDULED_WRITER_SOURCE_REGISTRY_IDS` | `{"michalowice-komunikaty":"11111111-1111-4111-8111-111111111111"}` | Maps the sourceKey to the synthetic canary-stand-in row's id on `alertownik-preview` |
| `SUPABASE_SCHEDULED_WRITER_EMAIL` | `preview-test-writer@example.invalid` | Existing, non-secret Preview test account identity (Sprint 165C Phase 4) |

## Canary source identifier (read-only, confirmed via live GET request, not SQL)

- `alert_sources.id` = `11111111-1111-4111-8111-111111111111`
- `name`: "Testowy Urząd Przykładowa (SYNTHETIC)" — its own notes field: "stand-in for the real Michałowice canary source"
- `url`: `https://example-preview-only.test/komunikaty`
- `is_active`: `true`

Confirmed via the already-authenticated Preview admin session's own GET request to `alertownik-preview`'s REST API — no SQL, no write, no session/password exposure.

## Deployment result

- Empty commit `84458f3` ("chore: trigger disabled preview canary baseline deployment") pushed to `sprint-166b-preview-canary-disabled-infrastructure-v1`.
- New deployment: `Ready`, environment `Preview`, branch and commit matched, no build errors, no manual Redeploy performed.

## QA result (read-only, all GET)

- All 6 public pages (`/`, `/alerty`, `/odpady`, `/wiecej`, `/ustawienia`, `/instalacja`): clean, no console errors, synthetic data only.
- `/admin`, `/admin/sources`: badge `PREVIEW` (never `PRODUCTION`), no console/hydration/CSP errors.
- All Supabase network calls confirmed `GET` only, targeting `nowvcdbtgaigutyxpmdp.supabase.co` (the isolated Preview project) exclusively.
- The null-URL synthetic source ("Testowe Wodociągi Przykładowa (SYNTHETIC, no URL)") renders correctly with its "⚠ Brak adresu URL" badge — no crash.
- "Stan automatyzacji (canary)" panel shows the writer inactive, consistent with both kill switches being explicitly `false`.
- Zero POST/PUT/PATCH/DELETE requests observed. No cron or `write-candidates` endpoint was called. Production was not opened or modified during this QA pass.

## Automation status: OFF

`SCHEDULED_CHECKS_ENABLED=false` and `SCHEDULED_WRITES_ENABLED=false` on Preview — both explicitly set, not merely unset. Even once this branch's environment variables take effect on every future Preview deployment, `write-candidates` remains fully inert until both are flipped to `true` in a separate, later, explicitly-approved step.

## Secrets configured in a later phase of this sprint

- `CRON_SECRET` (Preview scope, all Preview branches) — freshly generated by Adam locally (PowerShell `RandomNumberGenerator`, 48 bytes, base64), copied only via the Windows clipboard, pasted directly into Vercel by Adam. Never seen or typed by Claude. Rotated once mid-sprint (old value fully replaced); Production's own separate `CRON_SECRET` was never read or touched.
- `SUPABASE_SCHEDULED_WRITER_PASSWORD` (Preview scope, all Preview branches) — the existing Preview scheduled-writer account's password, pasted directly by Adam. Never seen by Claude. No account reset performed.

## Kill switches: activated, then returned to OFF

`SCHEDULED_CHECKS_ENABLED` and `SCHEDULED_WRITES_ENABLED` were each flipped to `true` (Preview scope only) in two separate, explicitly-approved steps, each followed by a natural deployment (empty commit + push, never a manual Redeploy) so the new values took effect. After the single canary write below completed successfully, `SCHEDULED_WRITES_ENABLED` was set back to `false` (Preview scope only) and another natural deployment confirmed the route is fail-closed again. `SCHEDULED_CHECKS_ENABLED` intentionally remains under separate consideration (see closure doc). Production's own copies of both variables were never read or modified at any point.

## Canary write execution result (this sprint's core deliverable)

**Dry-run first (`GET /api/cron/check-michalowice`):**
```json
{"ok":true,"dryRun":true,"checkedSources":1,"successfulSources":1,"failedSources":0,"totalProposalCount":6,"savedCandidates":0,"savedSourceChecks":0,"published":false}
```
Zero writes confirmed both by the response and by an unchanged table-count baseline on `alertownik-preview` (`alerts`=7, `source_notice_candidates`=3, `source_checks`=3, before and after).

**Then exactly one controlled write (`GET /api/cron/write-candidates`)**, triggered by Adam from his own terminal with the Preview `CRON_SECRET` (never seen by Claude):
```json
{"ok":true,"dryRun":false,"checkedSources":1,"successfulSources":1,"failedSources":0,"proposalsFound":6,"candidatesInserted":1,"duplicatesSkipped":0,"ambiguousCandidates":0,"cappedSkipped":5,"sourceChecksInserted":1,"duplicatesPreventedByDatabase":0,"published":false}
```

Read-only verification on `alertownik-preview` immediately after:

| Check | Before | After | Result |
|---|---|---|---|
| `alerts` | 7 | 7 | ✅ unchanged |
| `source_notice_candidates` | 3 | 4 | ✅ +1, hard cap respected (6 proposals found, 5 capped) |
| `source_checks` | 3 | 4 | ✅ +1 |
| New candidate `status` | — | `pending` | ✅ |
| New candidate `verification_status` | — | `unverified` | ✅ |
| New candidate `source_id` | — | `11111111-1111-4111-8111-111111111111` (the SYNTHETIC canary stand-in source) | ✅ |
| `alerts` published | — | `false` | ✅ no alert created or published |
| Supabase project touched | — | `nowvcdbtgaigutyxpmdp` (`alertownik-preview`) only, confirmed via SQL Editor and network requests | ✅ |
| Production | — | not opened, not queried, not modified | ✅ |

**Verdict: PASS on every criterion in `docs/SPRINT_166A_PREVIEW_CANARY_REHEARSAL_AUDIT_AND_DESIGN_V1.md` §F.4.** The new candidate remains `pending` in `alertownik-preview`, awaiting manual admin review via the existing `/admin/sources` → `/ai-helper` → `/builder` flow — no different from a manually-saved candidate. It was deliberately **not** deleted as part of this sprint's closure (Adam's explicit instruction) — it is a legitimate artifact of a successful, approved rehearsal, not test debris.

## Rollback procedure

Every variable set this sprint can be deleted individually from Vercel's Environment Variables page (Preview scope) with no code change, no redeploy requirement beyond the next natural deployment, and no effect on Production. `SCHEDULED_WRITES_ENABLED` is back to `false` as of this sprint's closure, so the write route is fail-closed again without needing any further action. The one `pending` candidate row created by the canary write can be deleted directly on `alertownik-preview` (SQL Editor or Table Editor) at any time, independent of this rollback section, if a future session decides it's no longer needed.
