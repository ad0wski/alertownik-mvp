# Sprint 166B — Final Acceptance: First Controlled Canary Write

**Status:** The candidate-automation pipeline's first real, controlled write on the isolated Preview environment is complete, verified, and accepted at the branch level. Branch `sprint-166b-preview-canary-disabled-infrastructure-v1`, **not yet merged to `main`** — that merge is a separate, later, Adam-only decision. Production (`alertownik-mvp`) has not been touched at any point across the entire sprint. Both kill switches are `false` again as of this document.

---

## What happened, in order

1. **Preflight (Sprint 166A carried forward):** full read-only audit of the existing pipeline, design for the smallest safe first canary — nothing activated (docs only).
2. **Non-secret Preview configuration (Sprint 166B, first phase):** seven non-secret variables set on Vercel, Preview scope only, both kill switches explicitly `false`. Verified live via a new deployment — badge `PREVIEW`, all Supabase calls GET-only, zero writes.
3. **Secrets configured (this session, later phase):** `CRON_SECRET` freshly generated by Adam (PowerShell `RandomNumberGenerator`, clipboard-only, rotated once mid-sprint) and `SUPABASE_SCHEDULED_WRITER_PASSWORD` (the existing Preview scheduled-writer account's real password) — both pasted directly into Vercel by Adam, both Preview scope only, all Preview branches, neither ever seen or typed by Claude.
4. **Layer 1 activated:** `SCHEDULED_CHECKS_ENABLED=true` (Preview only), approved explicitly, deployed via an empty commit (never a manual Redeploy). Confirmed live: `GET /api/cron/check-michalowice` moved from a `503` kill-switch message to a `401 Unauthorized` (bearer-token check now reachable).
5. **Dry-run:** `GET /api/cron/check-michalowice`, authenticated by Adam from his own terminal, returned `ok:true, dryRun:true, totalProposalCount:6, savedCandidates:0, savedSourceChecks:0, published:false`. Table counts on `alertownik-preview` unchanged before/after (`alerts`=7, `source_notice_candidates`=3, `source_checks`=3).
6. **Layer 2 activated:** `SCHEDULED_WRITES_ENABLED=true` (Preview only), approved explicitly, deployed via another empty commit.
7. **First controlled write:** `GET /api/cron/write-candidates`, authenticated by Adam from his own terminal with the Preview `CRON_SECRET`, returned:
   ```json
   {"ok":true,"dryRun":false,"checkedSources":1,"successfulSources":1,"failedSources":0,"proposalsFound":6,"candidatesInserted":1,"duplicatesSkipped":0,"ambiguousCandidates":0,"cappedSkipped":5,"sourceChecksInserted":1,"duplicatesPreventedByDatabase":0,"published":false}
   ```
8. **Read-only verification (immediately after):** table counts on `alertownik-preview` moved exactly as expected — `alerts` 7→7, `source_notice_candidates` 3→4, `source_checks` 3→4. The new row: `status='pending'`, `verification_status='unverified'`, `source_id='11111111-1111-4111-8111-111111111111'` (the synthetic canary stand-in source), `created_at` matching the response's `checkedAt`. No second insert, no `alerts` row created.
9. **Rollback to safe state:** `SCHEDULED_WRITES_ENABLED` returned to `false` (Preview only), deployed via a third empty commit. Confirmed live: `GET /api/cron/write-candidates` now returns `{"ok":false,"error":"Tryb zapisu jest wyłączony."}` — fail-closed again, before any auth check is even reached.
10. **Documentation updated:** `docs/SPRINT_166B_DISABLED_CANARY_INFRASTRUCTURE_V1.md`, `docs/ROADMAP.md` (Stage 15b), `README.md`, and this closure document.
11. **Full regression suite:** `npm run check` (typecheck + lint + build) clean; `npm run test:e2e` 681/682 passing (the one failure, `themeSystem.spec.ts` "clicking Jasny overrides a dark system preference", is a pre-existing timing flake — confirmed passing in isolation on retry, unrelated to any change in this sprint, which touched zero application code); `npm run test:pwa` 17/17 passing; `git diff --check` clean.

## Isolation proof (carried forward from Sprint 165C, re-confirmed this session)

- Every SQL query and every HTTP call made this session targeted `nowvcdbtgaigutyxpmdp.supabase.co` (`alertownik-preview`) exclusively — confirmed via the Supabase SQL Editor's own project breadcrumb and via `read_network_requests` on the live Preview deployment.
- Production's own separate `CRON_SECRET` and `SCHEDULED_CHECKS_ENABLED` Vercel entries (Production scope) were never opened, read, or modified.
- Supabase MCP (connected to Production) was not used at any point in this sprint — every database check went through the Supabase Dashboard's own SQL Editor, scoped to `alertownik-preview`, or through the app's own public API responses.
- No merge to `main` occurred; `main` remained at `e4f3e23` throughout.

## What remains explicitly out of scope (deliberately not done)

- **The one `pending` candidate was not deleted.** It is a legitimate artifact of a successful, approved rehearsal — not test debris — and remains available for ordinary manual review via `/admin/sources` → `/ai-helper` → `/builder`, exactly like any manually-saved candidate.
- **`SCHEDULED_CHECKS_ENABLED` was left `true`.** Only `SCHEDULED_WRITES_ENABLED` (Layer 2) was returned to `false` this session, per Adam's explicit instruction. Whether to also return Layer 1 to `false`, or to move toward a real recurring schedule, is a separate future decision.
- **No recurring/scheduled activation** (e.g., adding `write-candidates` to `vercel.json`) was made or considered — the only trigger used throughout was a single, manually-authenticated call per step.
- **Merge to `main`** — this document records branch-level acceptance only, per the release-preflight assessment below.

## Known limitations carried forward

- Same Supabase Free Plan constraints on `alertownik-preview` as recorded in Sprint 165C (shared org-wide quotas, auto-pause on inactivity).
- Preview data remains 100% synthetic except for the one new candidate's *content*, which was fetched from the real, public Michałowice announcements page (as designed in Sprint 166A §B) — the candidate row itself lives only in the isolated Preview database, never in Production.
- Pre-existing `themeSystem.spec.ts` flake (see step 11 above) is unrelated to this sprint and not fixed here — out of scope for a Preview-infrastructure sprint.

---

## Release-preflight assessment

| Check | Result |
|---|---|
| `main` unchanged | ✅ `e4f3e23`, matches `origin/main`, untouched throughout |
| Branch pushed, matches origin | ✅ `sprint-166b-preview-canary-disabled-infrastructure-v1` |
| Not merged to `main` | ✅ confirmed via `git merge-base --is-ancestor` |
| `npm run check` | ✅ clean |
| `npm run test:e2e` | 🟡 681/682 — 1 pre-existing, unrelated, confirmed-flaky failure (not introduced by this sprint, which changed zero application code) |
| `npm run test:pwa` | ✅ 17/17 |
| `git diff --check` | ✅ clean |
| Production untouched | ✅ verified structurally and empirically throughout |
| Both kill switches | `SCHEDULED_CHECKS_ENABLED=true`, `SCHEDULED_WRITES_ENABLED=false` (Preview only) |
| Uncommitted working-tree state | `.gitignore` has one unrelated, pre-existing local modification (adds `.vercel`) and `.vscode/` is untracked — neither touched by or relevant to this sprint's commits |

**Verdict: this branch is docs/config-only (no application code changed) and safe to fast-forward-merge to `main` whenever Adam chooses**, with two notes for that future, separate decision:
1. Merging brings `SCHEDULED_CHECKS_ENABLED=true` into whatever branch scope Production's own deploy uses — but since these are Preview-scoped Vercel variables (not code), merging the *branch* has no effect on Production's environment variables at all. The variables live in Vercel per-environment, entirely independent of which git branch is merged.
2. The pre-existing `themeSystem.spec.ts` flake is not a release blocker for this branch (it existed before this sprint and is unrelated to Preview canary infrastructure) but is worth fixing in a future, separate sprint for suite hygiene.

This sprint does not start Sprint 166C, does not perform a second write, does not delete the created candidate, and does not enable any recurring/scheduled automation — all four explicitly deferred to a future, separate, Adam-approved decision.
